# Install only when Stage 3 starts; do not download model weights during setup.
# Resolve and freeze exact versions after the NVIDIA driver passes nvidia-smi.
torch>=2.2
torchvision>=0.17
ultralytics>=8.4
